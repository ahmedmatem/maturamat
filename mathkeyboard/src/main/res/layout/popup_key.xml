<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="@dimen/key_height"
    android:layout_margin="@dimen/popup_key_margin">

    <TextView
        android:id="@+id/key_text"
        android:layout_width="@dimen/key_image_size"
        android:layout_height="@dimen/key_image_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        android:gravity="center_horizontal|center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        android:textSize="@dimen/key_text_size"
        android:visibility="gone"
        tools:text="B"/>

    <ImageView
        android:id="@+id/key_image"
        android:layout_width="@dimen/key_image_size"
        android:layout_height="@dimen/key_image_size"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:visibility="gone"
        app:srcCompat="@drawable/abc_vector_test"
        android:contentDescription="@string/image_key_description"/>

</androidx.constraintlayout.widget.ConstraintLayout>